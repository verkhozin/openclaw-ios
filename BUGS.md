# Bugs -- CLiOS

## Critical (app broken)
- [ ] Сообщения перестают проходить (WebSocket теряет соединение без reconnect?)
- [ ] Сообщения агента и юзера перемешиваются -- роли путаются
- [ ] Первое сообщение в чате пропадает

## Cards & Capabilities
- [ ] Карточки не рендерятся (парсер есть, компонент не подключен к чату)
- [ ] System event с cards.v1 не доходит до агента (агент не видит capability)
- [ ] Prefix с cards.v1 в первое сообщение каждой сессии -- не реализовано

## Chat UI
- [ ] Typing indicator не убирается при получении ответа агента
- [ ] Название сессии не ставится (ни от юзера, ни от агента через card:session.title)
- [ ] Пустая страница при открытии нового чата
- [ ] Поле ввода не растягивается при многострочном тексте
- [ ] Анимация поля ввода дергается
- [ ] Клавиатура + черная рамка -- замедлить анимацию, синхронизировать подъем

## Sessions
- [ ] Список сессий не обновляется после нового сообщения
- [ ] Свайп удаления сессии сломан (кривая анимация / не удаляет)

## Auth
- [ ] Keypair генерируется заново при пересборке -- сохранять в Keychain
